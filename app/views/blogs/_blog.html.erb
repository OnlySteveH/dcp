  <div class="blog-post">
    
    <h2 class="blog-post-title"><%= link_to blog.title.upcase, @blog %></h2>
    <p class="blog-post-meta">
    	Published <%= time_ago_in_words(blog.created_at) %> ago
    </p>
    <p class="blog-post-meta">
    	<%= link_to blog.status, toggle_status_blog_path(blog) %> / 
    </p>
    <p><%= blog.body %></p>
  </div>
<tr>
  <td></td>

  <td></td>
  <td>
</td>

  <td><%= link_to 'Edit', edit_blog_path(blog) if logged_in?(:site_admin)%></td>
  <td><%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %></td>
</tr>  